import { createOperatorsStore } from './storeFactory';

/**
 * Operators Store
 * Manages operators data
 * Uses the store factory pattern for consistency
 */
export const useOperatorsStore = createOperatorsStore();
